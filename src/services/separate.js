import { Database } from './db.ts'

/**
 * @param {number} _num
 * @returns {number}
 */
function separate(_num) {
  const db = new Database()

  console.log('separate')
  return 1
}

export { separate }